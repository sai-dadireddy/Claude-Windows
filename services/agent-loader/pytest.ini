[pytest]
# Prevent pytest from treating this as a package
# Only collect tests from files starting with test_
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Only look in tests directory
testpaths = tests

# Disable implicit package discovery
norecursedirs = __pycache__ *.egg-info .git .tox
